// #include "Viewer.h"
#include "System.h"
// #include <GL/glew.h>
#include "utils.h"

int main()
{
    stbr::System *sys = new stbr::System();

    // Viewer::MeshViewer *a;
    // // Mesh_Viewer::MeshViewer1 *a;
    // a = new Viewer::MeshViewer();
    // // sample();
    // a->run();
    return 0;
}